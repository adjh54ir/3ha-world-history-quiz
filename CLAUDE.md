# 전역 지침
 
항상 한국어.
 
## 응답 스타일
- 서론/마무리 생략, 핵심부터.
- 작업 결과 보고만 표(파일별 1줄), 단순 답변은 산문.
- 수정 후 확인 응답 1줄. 코드 주석 최소화.
- 대형 파일은 채팅 출력 대신 파일 저장 후 경로만. 소규모 수정은 diff.
- 수정한 코드 전문 재출력 금지.

## 작업 지시 해석
- 화면별 불릿으로 전달됨. "———" = 화면 구분.
- ✅ = 화면 내 섹션 위치(서브타이틀). 그 아래 불릿이 해당 섹션의 작업 내용.
  ✅ 단독으로 아래 내용이 없으면 작업 아님, 무시.
- 내용 없는 화면(화면명만 있고 불릿 없음)도 무시.
- "공통" = 특정 화면이 아닌 앱 전체 적용. 관련된 모든 화면·전역 코드에 반영.
- 경로 안 줌. 화면명 + ✅ 섹션명으로 파일·코드 위치 추론. 재질문 금지,
  가장 유력한 위치로 착수 후 판단 근거 1줄만. 정말 못 찾을 때만 후보 2개 제시.
- 착수 전 작업 목록 3줄 이내 요약 후 바로 진행(승인 대기 없이).
- 한 화면 불릿 여러 개는 파일 한 번 열어 몰아 수정.

## 코드 작업 규칙
- "이쁘게/깔끔하게" = RN UI 표준 적용.
- 스타일 수정은 기존 색상·간격 상수/테마 값 재사용. 새 값 임의 도입 금지.
- 라이브러리 작업은 package.json 먼저 1회 확인, 미설치면 질문 없이 설치부터.
- 완료 기준: tsc 통과. 통과 후에만 완료 보고.
- 같은 수정이 3개 파일 이상 반복되면 sed/스크립트로 일괄 처리.
- 애드몹 작업은 임의 진행 금지. 변경 내용 요약 후 허락 받고 진행(템플릿 유지).

## 토큰 절약
- 폴더 전체 탐색 금지. 실패 시에만 grep.
- 파일 읽기는 범위 지정, 한 번에 200줄 초과 금지.
- 이미 연 파일·공유된 파일 재열람/재출력 금지.
- 빌드·테스트 출력은 tail -50만. 에러는 스택만.
- 정적 JSON(픽시리즈)·에셋 스크립트는 결과만, 원본 안 올림.
- 실패한 접근 반복 금지. 2회 실패 시 즉시 다른 방법 전환.
- 병렬 가능한 도구 호출은 한 턴에 묶어 실행.
- 광범위한 탐색이 필요하면 Task(서브에이전트)에 위임해 결론만 받는다.
- 무관한 웹검색·스킬·MCP 트리거 금지.

## 세션 관리
- 작업 단위로 세션 분리. 새 주제 = /clear.
- 세션 인계는 HANDOFF.md(완료·미결·주의 5줄 이내)로만.
- 중간 산출물은 파일 저장 후 경로 참조.