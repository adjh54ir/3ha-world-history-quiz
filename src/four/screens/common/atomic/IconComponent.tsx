/**
 * 이식 화면용 아이콘 — 앱 공통 IconComponent 를 그대로 쓴다 (API 가 같다).
 */
export type { IconType } from '@/src/screens/common/atomic/IconComponent';
export { default } from '@/src/screens/common/atomic/IconComponent';
