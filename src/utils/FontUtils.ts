/**
 * 한자 전용 서체 이름
 * -------------------------------------------------
 * 학습 도메인이 세계 상식으로 바뀌면서 한자를 그리는 화면이 남지 않았다.
 * 2.4MB 짜리 서체를 시작할 때마다 올릴 이유가 없어 로더는 걷어냈고,
 * 스타일(ConstDesign·useHanjaFont)이 아직 참조하는 패밀리 이름만 남긴다.
 * 이름을 못 찾은 Text 는 OS 기본 서체로 그려진다.
 */

/** 서체 패밀리 이름 — 스타일에서 이 이름으로 참조한다 (지금은 올리지 않으므로 기본 서체로 떨어진다) */
export const HANJA_FONT_NAME = 'HanpickHanjaSerif';
